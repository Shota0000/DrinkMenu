<!DOCTYPE html>
<html>

<head>
  <title>DrinkReview</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</head>

<body>
  <header>
    <div class="p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <nav class="navbar navbar-expand-md navbar-light ">
        <div class="container">
          <a class="navbar-left" href="/">
            <%= image_tag "icon.jpg",class:"icon navbar-brand"%>
          </a>
          <h5 class="title my-0 mr-md-auto font-weight-normal ">DrinkReview</h5>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
              <li class="nav-item">
                <%= form_with url: drinks_search_path, method: :get, local: true ,class: "form-inline" do |f| %>
                <%= f.text_field :name ,class: "form-control col-8"%>
                <%= f.submit :search,class: "btn btn-secondary col-4"%>
                <% end %>
              </li>
              <span>　</span>
              <li class="nav-item">
                <%= link_to "My page", user_path(current_user),class:"btn" %>
              </li>
              <span>　</span>
              <li class="nav-item">
                <%= link_to "Log out", destroy_user_session_path, method: :delete,class:"btn" %>
              </li>
              <% else %>
              <li class="nav-item">
                <%= link_to "Sign up", new_user_registration_path,class:"btn" %>
              </li>
              <span>　</span>
              <li class="nav-item">
                <%= link_to "Log in", new_user_session_path,class:"btn" %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
      <% if  notice%>
      <p class="notice alert alert-success"><%= notice %></p>
      <% end %>

  </header>
  <%= yield %>
  <footer>
    <div class="container mt-5">
      <div class="row">
        <div class="mx-auto">
          <p>© 2021 DrinkReview</p>
        </div>
      </div>
    </div>
  </footer>
</body>

</html>